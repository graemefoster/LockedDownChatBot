{
    "$schema": "https://schemas.botframework.com/schemas/component/v1.0/component.schema",
    "$role": "implements(Microsoft.IDialog)",
    "title": "SummariseConversation",
    "description": "Recognises Intent, or returns a suggested dialogue to get closer.",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "systemPrompt": {
            "$ref": "schema:#/definitions/stringExpression",
            "title": "SystemPrompt",
            "description": "System Prompt"
        },
        "conversation": {
            "$ref": "schema:#/definitions/arrayExpression",
            "title": "Conversation",
            "description": "Conversation to summarise"
        },
        "resultProperty": {
            "$ref": "schema:#/definitions/stringExpression",
            "title": "Result",
            "description": "Value from callers memory to store the result"
        }
    }
}