{
    "$schema": "https://schemas.botframework.com/schemas/component/v1.0/component.schema",
    "$role": "implements(Microsoft.IDialog)",
    "title": "OpenAiSuggestFunctionCall",
    "description": "Extracts key words to help create a function all.",
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "systemPrompt": {
            "$ref": "schema:#/definitions/stringExpression",
            "title": "SystemPrompt",
            "description": "System Prompt"
        },
        "function": {
            "$ref": "schema:#/definitions/stringExpression",
            "title": "Function",
            "description": "Json Schema representation of function"
        },
         "inputs": {
             "$ref": "schema:#/definitions/arrayExpression",
             "title": "Inputs to look for intent over",
             "description": "Pass in any text entered by the User to look for intent within"
         },
        "resultProperty": {
            "$ref": "schema:#/definitions/stringExpression",
            "title": "Result",
            "description": "Value from callers memory to store the result"
        }
    }
}